name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Bowire.Protocol.Surgewave.slnx

      - name: Build
        run: dotnet build Bowire.Protocol.Surgewave.slnx -c Release --no-restore

      - name: Test
        # XPlat Code Coverage drops one coverage.cobertura.xml per test
        # project under artifacts/coverage/<guid>/. codecov/codecov-action
        # globs them up directly — no merge step needed on our side.
        run: >
          dotnet test Bowire.Protocol.Surgewave.slnx -c Release --no-build -v normal
          --collect:"XPlat Code Coverage"
          --results-directory artifacts/coverage

      - name: Upload coverage to Codecov
        # Skip on fork PRs — the secret isn't injected for outside
        # contributors, the upload would fail noisily otherwise.
        if: ${{ !github.event.pull_request.head.repo.fork }}
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: artifacts/coverage
          files: '**/coverage.cobertura.xml'
          flags: unittests
          fail_ci_if_error: false
